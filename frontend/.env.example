# DevFlow Frontend Environment Variables

# The production URL of the Render backend API (with /api suffix)
VITE_API_URL=https://devflow-backend.onrender.com/api

# The production URL of the Render backend server (without /api suffix)
VITE_SOCKET_URL=https://devflow-backend.onrender.com
